<div class='categories-container'>
  <h1>All Categories</h1>
  <% @categories.each do |category| %>
    <%= link_to  category.title, "/#{category.slug}" %>
  <% end %>
  <h2>Wonderful categories, aren't they?</h2>
</div>
